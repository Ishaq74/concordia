---
import DocLayout from "@layouts/DocLayout.astro";
import { Code } from "astro:components";
import TooltipComponent from "@components/ui/Tooltip.astro";
import Button from "@components/ui/Button.astro";
import Badge from "@components/ui/Badge.astro";
import TableOfContents from "@/components/templates/docs/TableOfContents.astro";
import Table from "@components/ui/Table/Table.astro";
import TableHeader from "@components/ui/Table/TableHeader.astro";
import TableBody from "@components/ui/Table/TableBody.astro";
import TableRow from "@components/ui/Table/TableRow.astro";
import TableHead from "@components/ui/Table/TableHead.astro";
import TableCell from "@components/ui/Table/TableCell.astro";
---

<DocLayout 
  title="Tooltip Component" 
  description="Tooltip component documentation for displaying contextual tooltips on hover"
  variant="initial"
>
  <h1 id="tooltip-component">Tooltip Component</h1>
  
  <p>
    The <code class="inline-code">Tooltip</code> component displays a contextual tooltip on hover or focus of an element to provide additional information.
  </p>

  <TableOfContents
    items={[
      { label: "Installation", href: "#installation" },
      { label: "Props", href: "#props" },
      { label: "Basic Usage", href: "#basic" },
      { label: "Positions", href: "#positions" },
      { label: "Variants", href: "#variants" },
      { label: "Colors", href: "#colors" },
      { label: "With other components", href: "#with-components" },
      { label: "Accessibility", href: "#accessibility" },
    ]}
    title="Table of Contents"
  />

  <h2 id="installation">Installation</h2>

  <Code
    lang="astro"
    code={`import Tooltip from '@components/ui/Tooltip.astro';`}
  />

  <h2 id="props">Props</h2>

  <div class="props-table">
    <Table striped>
      <TableHeader>
        <TableRow>
          <TableHead>Prop</TableHead>
          <TableHead>Type</TableHead>
          <TableHead>Default</TableHead>
          <TableHead>Description</TableHead>
        </TableRow>
      </TableHeader>
      <TableBody>
        <TableRow>
          <TableCell><code class="inline-code">variant</code></TableCell>
          <TableCell>initial | retro | modern | futuristic</TableCell>
          <TableCell>initial</TableCell>
          <TableCell>Visual style of the tooltip</TableCell>
        </TableRow>
        <TableRow>
          <TableCell><code class="inline-code">color</code></TableCell>
          <TableCell>default | primary | secondary | accent</TableCell>
          <TableCell>default</TableCell>
          <TableCell>Tooltip color</TableCell>
        </TableRow>
        <TableRow>
          <TableCell><code class="inline-code">text</code></TableCell>
          <TableCell>string</TableCell>
          <TableCell>-</TableCell>
          <TableCell>Tooltip text (required)</TableCell>
        </TableRow>
        <TableRow>
          <TableCell><code class="inline-code">position</code></TableCell>
          <TableCell>top | bottom | left | right</TableCell>
          <TableCell>top</TableCell>
          <TableCell>Tooltip position relative to the element</TableCell>
        </TableRow>
        <TableRow>
          <TableCell><code class="inline-code">className</code></TableCell>
          <TableCell>string</TableCell>
          <TableCell>""</TableCell>
          <TableCell>Additional CSS classes</TableCell>
        </TableRow>
      </TableBody>
    </Table>
  </div>

  <h2 id="basic">Basic Usage</h2>

  <p>Wrap any content in a Tooltip to display a tooltip on hover:</p>

  <div class="example-section">
    <div style="display: flex; gap: 2rem; flex-wrap: wrap;">
      <TooltipComponent text="This is a tooltip">
        <span style="border-bottom: 1px dashed var(--text-muted);">Hover me</span>
      </TooltipComponent>
      
      <TooltipComponent text="Important information">
        <Button>Button with tooltip</Button>
      </TooltipComponent>
      
      <TooltipComponent text="Active status">
        <Badge color="primary">Active</Badge>
      </TooltipComponent>
    </div>
  </div>

  <Code
    lang="astro"
    code={`<TooltipComponent text="This is a tooltip">
  <span>Hover me</span>
</TooltipComponent>

<TooltipComponent text="Important information">
  <Button>Button with tooltip</Button>
</TooltipComponent>`}
  />

  <h2 id="positions">Positions</h2>

  <p>The tooltip can be positioned at the top, bottom, left, or right of the element:</p>

  <div class="example-section">
    <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(150px, 1fr)); gap: 2rem; padding: 3rem;">
      <TooltipComponent text="Top position" position="top">
        <Button>Top</Button>
      </TooltipComponent>
      
      <TooltipComponent text="Bottom position" position="bottom">
        <Button>Bottom</Button>
      </TooltipComponent>
      
      <TooltipComponent text="Left position" position="left">
        <Button>Left</Button>
      </TooltipComponent>
      
      <TooltipComponent text="Right position" position="right">
        <Button>Right</Button>
      </TooltipComponent>
    </div>
  </div>

  <Code
    lang="astro"
    code={`<TooltipComponent text="Top position" position="top">
  <Button>Top</Button>
</TooltipComponent>

<TooltipComponent text="Bottom position" position="bottom">
  <Button>Bottom</Button>
</TooltipComponent>

<TooltipComponent text="Left position" position="left">
  <Button>Left</Button>
</TooltipComponent>

<TooltipComponent text="Right position" position="right">
  <Button>Right</Button>
</TooltipComponent>`}
  />

  <h2 id="variants">Variants</h2>

  <h3>Initial (default)</h3>
  <div class="example-section">
    <TooltipComponent variant="initial" text="Tooltip with initial variant">
      <Button variant="initial">Hover me (Initial)</Button>
    </TooltipComponent>
  </div>

  <h3>Retro</h3>
  <div class="example-section">
    <TooltipComponent variant="retro" text="Pixelated tooltip, 80s style">
      <Button variant="retro">Hover me (Retro)</Button>
    </TooltipComponent>
  </div>

  <h3>Modern</h3>
  <div class="example-section">
    <TooltipComponent variant="modern" text="Minimalist tooltip with backdrop blur">
      <Button variant="modern">Hover me (Modern)</Button>
    </TooltipComponent>
  </div>

  <h3>Futuristic</h3>
  <div class="example-section">
    <TooltipComponent variant="futuristic" text="Cyberpunk tooltip with neon effect">
      <Button variant="futuristic">Hover me (Futuristic)</Button>
    </TooltipComponent>
  </div>

  <Code
    lang="astro"
    code={`<TooltipComponent variant="initial" text="Tooltip initial">
  <Button variant="initial">Initial</Button>
</TooltipComponent>

<TooltipComponent variant="retro" text="Tooltip retro">
  <Button variant="retro">Retro</Button>
</TooltipComponent>

<TooltipComponent variant="modern" text="Tooltip modern">
  <Button variant="modern">Modern</Button>
</TooltipComponent>

<TooltipComponent variant="futuristic" text="Tooltip futuristic">
  <Button variant="futuristic">Futuristic</Button>
</TooltipComponent>`}
  />

  <h2 id="colors">Colors</h2>

  <p>Tooltips can have different colors to indicate the type of information:</p>

  <div class="example-section">
    <div style="display: flex; gap: 2rem; flex-wrap: wrap;">
      <TooltipComponent color="default" text="Default information">
        <Button>Default</Button>
      </TooltipComponent>
      
      <TooltipComponent color="primary" text="Primary information">
        <Button color="primary">Primary</Button>
      </TooltipComponent>
      
      <TooltipComponent color="secondary" text="Secondary information">
        <Button color="secondary">Secondary</Button>
      </TooltipComponent>
      
      <TooltipComponent color="accent" text="Warning or alert">
        <Button color="accent">Accent</Button>
      </TooltipComponent>
    </div>
  </div>

  <Code
    lang="astro"
    code={`<TooltipComponent color="default" text="Default information">
  <Button>Default</Button>
</TooltipComponent>

<TooltipComponent color="primary" text="Primary information">
  <Button color="primary">Primary</Button>
</TooltipComponent>

<TooltipComponent color="secondary" text="Secondary information">
  <Button color="secondary">Secondary</Button>
</TooltipComponent>

<TooltipComponent color="accent" text="Warning or alert">
  <Button color="accent">Accent</Button>
</TooltipComponent>`}
  />

  <h2 id="with-components">With other components</h2>

  <p>The Tooltip works with all components of the design system:</p>

  <div class="example-section">
    <div style="display: flex; gap: 2rem; flex-wrap: wrap; align-items: center;">
      <TooltipComponent text="Badge with tooltip">
        <Badge color="primary">Badge</Badge>
      </TooltipComponent>
      
      <TooltipComponent text="Click to save" position="bottom">
        <Button color="primary">Save</Button>
      </TooltipComponent>
      
      <TooltipComponent text="Dangerous action" position="right">
        <Button color="accent">Delete</Button>
      </TooltipComponent>

      <TooltipComponent text="Version 2.5.0" position="left">
        <Badge color="secondary">v2.5.0</Badge>
      </TooltipComponent>
    </div>
  </div>

  <Code
    lang="astro"
    code={`<TooltipComponent text="Badge with tooltip">
  <Badge color="primary">Badge</Badge>
</TooltipComponent>

<TooltipComponent text="Click to save" position="bottom">
  <Button color="primary">Save</Button>
</TooltipComponent>`}
  />

  <h2 id="accessibility">Accessibility</h2>

  <ul>
    <li><strong>ARIA Role:</strong> The tooltip uses <code class="inline-code">role="tooltip"</code></li>
    <li><strong>Keyboard:</strong> Shows on focus (Tab) and hides on blur</li>
    <li><strong>Hover & Focus:</strong> Works with mouse and keyboard</li>
    <li><strong>Cursor:</strong> Help cursor to indicate information is available</li>
    <li><strong>High z-index:</strong> Displays above all other content (z-index: 9999)</li>
    <li><strong>Pointer-events:</strong> Tooltip does not interfere with interactions</li>
  </ul>

  <h2>Best Practices</h2>

  <ul>
    <li><strong>Short text:</strong> Limit the text to one or two sentences</li>
    <li><strong>Supplementary info:</strong> Do not put critical information only in a tooltip</li>
    <li><strong>Smart position:</strong> Choose the position to avoid going off-screen</li>
    <li><strong>Consistency:</strong> Use the same variant as your other components</li>
    <li><strong>Mobile:</strong> On mobile, prefer alternative solutions (hover tooltips do not work well)</li>
  </ul>

  <h2>Full Example</h2>

  <div class="example-section">
    <div style="display: flex; flex-direction: column; gap: 2rem; max-width: 600px;">
      <div style="display: flex; gap: 1rem; align-items: center;">
        <TooltipComponent text="Save changes" position="top">
          <Button color="primary">Save</Button>
        </TooltipComponent>
        
        <TooltipComponent text="Cancel and go back" position="top">
          <Button>Cancel</Button>
        </TooltipComponent>
        
        <TooltipComponent text="Delete permanently (irreversible)" position="top">
          <Button color="accent">Delete</Button>
        </TooltipComponent>
      </div>

      <div style="border: 1px solid var(--border-default); padding: 1rem; border-radius: var(--border-radius-md);">
        <p style="margin: 0;">
          User: 
          <TooltipComponent text="John Doe - Administrator" position="right">
            <strong style="border-bottom: 1px dashed var(--text-muted);">John Doe</strong>
          </TooltipComponent>
        </p>
        <p style="margin: 0.5rem 0 0 0;">
          Status: 
          <TooltipComponent text="Account active since 2024" position="right">
            <Badge color="primary">Active</Badge>
          </TooltipComponent>
        </p>
      </div>
    </div>
  </div>

  <Code
    lang="astro"
    code={`<div>
  <TooltipComponent text="Save changes" position="top">
    <Button color="primary">Save</Button>
  </TooltipComponent>
  
  <TooltipComponent text="Cancel and go back" position="top">
    <Button>Cancel</Button>
  </TooltipComponent>
</div>

<p>
  User: 
  <TooltipComponent text="John Doe - Administrator" position="right">
    <strong>John Doe</strong>
  </TooltipComponent>
</p>`}
  />
</DocLayout>