---
interface Props {
  variant?: 'initial' | 'retro' | 'modern' | 'futuristic';
}

const { variant = 'initial' } = Astro.props;
---

<article class={`blog-main variant-${variant}`}>
  <slot />
</article>

<style>
  .blog-main {
    min-width: 0;
    max-width: 800px;
  }
</style>