---
import Card from "@components/ui/Card/Card.astro";
import { getLocalizedUrl } from "@lib/i18n/route-helpers";

interface Props {
  count: number;
  translations: Record<string, any>;
  lang?: string;
}
const { count, translations } = Astro.props as Props;
const lang = (Astro.props as Props).lang ?? Astro.currentLocale ?? "fr";
---

<Card variant="modern" elevation="sm" className="mt-6 p-4">
  <p class="text-sm font-medium text-slate-500">{translations.auth.invitation ?? 'Invitations'}</p>
  <p class="text-lg font-semibold text-slate-900">
    <a
      class="text-slate-900 underline decoration-slate-300 underline-offset-2 hover:decoration-slate-600"
      href={getLocalizedUrl(lang, "auth/invitations")}
    >
      {count > 0 ? `${count} ${translations.profile?.pending ?? "en attente"}` : translations.auth.noInvitations ?? 'Aucune'}
    </a>
  </p>
</Card>
