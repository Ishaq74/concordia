---
/**
 * TabPanel - Panneau de contenu CSS-only (ZERO SCRIPT)
 * 
 * Position détermine le matching avec Tab (pas d'ID en dur)
 * 
 * @param {string} className - Classes CSS additionnelles
 */

interface Props {
  className?: string;
}

const {
  className = '',
} = Astro.props;

const classes = [
  'tab-panel',
  className,
].filter(Boolean).join(' ');
---

<div class={classes}>
  <slot />
</div>
