---
import { Icon } from 'astro-icon/components';

interface Props {
  variant?: "initial" | "retro" | "modern" | "futuristic";
  status?: "info" | "success" | "warning" | "danger" | "error";
  title?: string;
  message?: string;
  icon?: string;
  dismissible?: boolean;
  className?: string;
}

const {
  variant = "initial",
  status = "info",
  title,
  message,
  icon,
  dismissible = false,
  className = "",
} = Astro.props;

const classes = [
  "alert",
  variant !== "initial" && variant,
  status,
  className,
]
  .filter(Boolean)
  .join(" ");

// Icônes par défaut selon le statut
const defaultIcons = {
  info: "mdi:information",
  success: "mdi:check-circle",
  warning: "mdi:alert",
  danger: "mdi:alert-circle",
  error: "mdi:alert-circle",
};

const displayIcon = icon || defaultIcons[status];
---

<div class={classes} data-status={status} role="alert">
  {displayIcon && <Icon name={displayIcon} />}
  <div>
    {title && <strong>{title}</strong>}
    {message ? message : <slot />}
  </div>
  {dismissible && (
    <button 
      type="button" 
      class="alert-close"
      aria-label="Fermer l'alerte"
      onclick="this.closest('.alert').remove()"
    >
      <Icon name="mdi:close" />
    </button>
  )}
</div>

<style>
.alert { 
  padding: var(--alert-padding-y) var(--alert-padding-x); 
  border-radius: var(--alert-border-radius); 
  display: flex; 
  align-items: center; 
  border: var(--alert-border-width) solid transparent; 
  width: 100%; 
  margin-bottom: 0.5rem; 
  position: relative;
  overflow: hidden;
}
.alert[data-status="info"], .alert.info { background: var(--alert-info-bg); color: var(--alert-info-color); border-color: var(--alert-info-border); }
.alert[data-status="success"], .alert.success { background: var(--alert-success-bg); color: var(--alert-success-color); border-color: var(--alert-success-border); }
.alert[data-status="warning"], .alert.warning { background: var(--alert-warning-bg); color: var(--alert-warning-color); border-color: var(--alert-warning-border); }
.alert[data-status="danger"], .alert[data-status="error"], .alert.danger, .alert.error { background: var(--alert-error-bg); color: var(--alert-error-color); border-color: var(--alert-error-border); }

.alert-close {
  background: none;
  border: none;
  cursor: pointer;
  padding: 0;
  margin-left: 0.75rem;
  display: inline-flex;
  align-items: center;
  opacity: 0.7;
  transition: opacity 0.2s;
}

.alert-close:hover {
  opacity: 1;
}

.alert-progress {
  position: absolute;
  bottom: 0;
  left: 0;
  height: 3px;
  width: 100%;
  background: currentColor;
  opacity: 0.3;
  transform-origin: left;
}

@keyframes alert-progress {
  from {
    transform: scaleX(1);
  }
  to {
    transform: scaleX(0);
  }
}

/* ALERT RETRO */
.alert.retro { 
  border-radius: 0; 
  border: var(--retro-border-width) solid currentColor; 
  background: var(--retro-alert-bg); 
  box-shadow: 4px 4px 0 var(--retro-shadow-color); 
}
.alert.retro[data-status="info"] { color: var(--color-secondary); }
.alert.retro[data-status="success"] { color: var(--color-success); }
.alert.retro[data-status="warning"] { color: var(--color-warning); }
.alert.retro[data-status="danger"] { color: var(--color-danger); }
.alert.retro[data-status="error"] { color: var(--color-danger); }

/* ALERT MODERN */
.alert.modern { 
  border-radius: var(--border-radius-md); 
  background: var(--modern-alert-bg); 
  border-left: 4px solid currentColor; 
  box-shadow: var(--shadow-sm); 
}
.alert.modern[data-status="info"] { color: var(--color-secondary); }
.alert.modern[data-status="success"] { color: var(--color-success); }
.alert.modern[data-status="warning"] { color: var(--color-warning); }
.alert.modern[data-status="danger"] { color: var(--color-danger); }
.alert.modern[data-status="error"] { color: var(--color-danger); }

/* ALERT FUTURISTIC */
.alert.futuristic { 
  border-radius: 0; 
  clip-path: var(--futuristic-clip-path); 
  border: 1px solid currentColor; 
  background: var(--futuristic-alert-bg); 
  backdrop-filter: blur(4px); 
  position: relative; 
}
.alert.futuristic::before { 
  content: ''; 
  position: absolute; 
  left: 0; 
  top: 0; 
  width: 4px; 
  height: 100%; 
  background: currentColor; 
  box-shadow: 0 0 10px currentColor; 
}
.alert.futuristic[data-status="info"] { color: var(--color-secondary); }
.alert.futuristic[data-status="success"] { color: var(--color-success); }
.alert.futuristic[data-status="warning"] { color: var(--color-warning); }
.alert.futuristic[data-status="danger"] { color: var(--color-danger); }
.alert.futuristic[data-status="error"] { color: var(--color-danger); }
</style>