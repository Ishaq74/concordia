---
interface Props {
  className?: string;
}

const {
  className = "",
} = Astro.props;

const classes = [
  "form-group",
  className,
]
  .filter(Boolean)
  .join(" ");
---

<div class={classes}>
  <slot />
</div>
